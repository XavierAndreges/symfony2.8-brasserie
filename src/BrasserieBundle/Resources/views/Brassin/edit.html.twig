{% extends '::base.html.twig' %}

{% block body -%}

    <h1>Brassin edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">

        <li>
            <a href="{{ path('brassin') }}">
                Back to the list
            </a>
            <a href="{{ path('brassin_duplicate') }}">
                Duplicate
            </a>
        </li>

    <li>{{ form(delete_form) }}</li>

    </ul>
    
{% endblock %}
