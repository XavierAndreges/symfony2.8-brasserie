{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Levure list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Quantité</th>
                <th>Style</th>
                <th>Description</th>
                <th>Commentaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('levure_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.quantite }}</td>
                <th>{{ entity.styleDeBiere }}</th>
                <td>{{ entity.description|trans|nl2br }}</td>
                <td>{{ entity.commentaire|trans|nl2br }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('levure_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('levure_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('levure_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
