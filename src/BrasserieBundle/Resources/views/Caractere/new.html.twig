{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Caractere creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('caractere') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
